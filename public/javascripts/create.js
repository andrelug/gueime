$(function()
{
    $('#content').redactor({
        lang: 'pt_br',
        toolbarFixed: true,
        air: true,
        airButtons: ['formatting', 'bold', 'italic', 'deleted', 'unorderedlist', 'orderedlist', 'outdent', 'indent']
    });
});